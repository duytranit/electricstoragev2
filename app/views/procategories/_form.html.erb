<%= form_for(@procategory) do |f| %>
  <% if @procategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@procategory.errors.count, "error") %> prohibited this procategory from being saved:</h2>

      <ul>
      <% @procategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
